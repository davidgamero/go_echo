apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "go_echo.fullname" . }}-config
  labels:
    {{- include "go_echo.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
data:
{{- range $key, $value := .Values.envVars }}
  {{ $key }}: {{ $value }}
{{- end }}
